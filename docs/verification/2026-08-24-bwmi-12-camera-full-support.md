# BWMI-12 verification: indoor IP camera full support

**Verified:** 2026-08-24  
**Terminal result:** **admitted and promoted to `full_support`**  
**Exact pack:** `india-retail-indoor-wifi-ip-camera-v1`  
**Public label:** Indoor IP camera  
**Admitted HS mapping:** `85258900`

## Scope and admission result

The provisional camera was narrowed to one new, finished, single-model indoor 2.4/5 GHz Wi-Fi IP security camera with one dedicated external DC adapter. Battery, Ethernet/PoE, integrated recorder, 6 GHz, cellular, other radio, outdoor/industrial/special-subheading cameras, modules, bundles and ambiguous or used sets are outside the pack.

Independent official sources establish the residual camera mapping, 20% BCD, nil AIDC, 10% SWS, 18% IGST and nil compensation cess; exact-model WPC ETA/undertaking; separate camera and adapter BIS conditions; REPA; package declarations; and the dated exact-party trade-remedy check. The ₹100,000 fixture total is ₹43,960. No router or headphones identifier is accepted as camera evidence.

Primary admission record: `research/indoor-wifi-ip-camera-product-pack-primary-sources-2026-08-24.md`.

## Test-first evidence

The camera contract and three-product expectations were added first. The initial focused run failed as intended because `loadCameraPack` did not exist and the catalog still contained two products: 12 failed, 4 passed.

After implementation:

- Focused camera, headphones, public-promotion and engine gate: 24/24 passed.
- Full Vitest gate: 33/33 passed across 5 files.
- Strict TypeScript: passed with no diagnostics.
- ESLint: passed with no diagnostics.
- Next.js 16.3 optimized production build: passed; `/`, `/api/preflight`, `/promotion-harness` compiled.
- Impeccable mechanical design detector over changed UI targets: no findings.

## Complete browser evidence

Playwright ran 22/22 checks successfully across 1280×900 desktop and 360×800 mobile:

- Router: Ready, Blocked and Needs verification at both widths.
- Bluetooth headphones: Ready, Blocked and Needs verification at both widths.
- Indoor IP camera: Ready, Blocked and Needs verification at both widths.
- Exactly three public radio choices at both widths.
- Keyboard skip-link journey at both widths.
- Axe WCAG 2 A/AA and 2.1 A/AA scan with zero violations at both widths.
- No horizontal overflow at either width.
- Restricted promotion harness uses the shared journey and preserves source-only status there.

The captured camera Ready pages were visually reviewed at both widths. The third selector choice, exact scope, adapter identity, five non-trade evidence controls, result hierarchy, HS mapping and cost remain legible and consistent with the existing Operate evidence-ledger direction.

## Production behavior and process cleanup

The built app was started temporarily on `127.0.0.1:3210` with the promotion-harness flag absent:

- `GET /` returned 200 and rendered Indoor IP camera, `85258900` and `full_support`.
- `GET /promotion-harness` returned 404.
- Public `POST /api/preflight` for the exact Ready camera returned 200, product pack `india-retail-indoor-wifi-ip-camera-v1`, outcome `ready`, HS `85258900`, and total import duties `43960.00`.

The exact agent-started production server process was terminated with SIGTERM after smoke testing. It exited 143, did not respawn, and port 3210 was verified clear. No pre-existing application listener was stopped.

## Isolation result

Camera source, rule, action, evidence and fixture IDs are camera-owned. Contract tests reject camera evaluation of router/headphones facts and reject any source, rule, action, fixture, mapping, rate or decision overlap. The complete legacy router and headphones unit and browser matrices remain green.

This is the only BWMI-12 terminal result: the camera is independently admitted and publicly promoted. No fourth product, PDF path, image capture, Other-product path or BWMI-13 implementation is included.
